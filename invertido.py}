def invertir_cadena(texto):
    invertida = ""
    for i in range(len(texto) - 1, -1, -1):
        invertida += texto[i]
    return invertida

# Prueba
cadena = "Hola mundo"
resultado = invertir_cadena(cadena)
print(resultado)
